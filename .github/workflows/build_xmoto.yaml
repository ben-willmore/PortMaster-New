name: Build Xmoto
on:
  # Build on request
  workflow_dispatch
#  schedule:
#    - cron:  '5 4 * * *'

jobs:
  build:
    uses: ./.github/workflows/build_port.yaml
    with:
      port-name: "Xmoto"
      short-name: Xmoto
      port-dir: xmoto
      build-type: release
      email: ben@willmore.eu
