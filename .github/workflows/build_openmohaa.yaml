name: Build OpenMoHAA
on:
  # Build on request
  workflow_dispatch
#  schedule:
#    - cron:  '5 4 * * *'

jobs:
  build:
    uses: ./.github/workflows/build_port.yaml
    with:
      port-name: "OpenMoHAA"
      short-name: OpenMoHAA
      port-dir: openmohaa
      build-type: release
      email: ben@willmore.eu
