name: Build Arx Libertatis
on:
  # Build on request
  workflow_dispatch
#  schedule:
#    - cron:  '5 4 * * *'

jobs:
  build:
    uses: ./.github/workflows/build_port.yaml
    with:
      port-name: "Arx"
      short-name: Arx
      port-dir: arx
      build-type: release
      email: ben@willmore.eu
