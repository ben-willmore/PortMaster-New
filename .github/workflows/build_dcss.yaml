name: Build Dungeon Crawl Stone Soup
on:
  # Build on request
  workflow_dispatch
#  schedule:
#    - cron:  '5 4 * * *'

jobs:
  build:
    uses: ./.github/workflows/build_port.yaml
    with:
      port-name: "Dungeon Crawl Stone Soup"
      short-name: DCSS
      port-dir: dungeoncrawlstonesoup
      build-type: release
      email: ben@willmore.eu
