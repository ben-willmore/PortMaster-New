name: Build OpenNotrium
on:
  # Build on request
  workflow_dispatch
#  schedule:
#    - cron:  '5 4 * * *'

jobs:
  build:
    uses: ./.github/workflows/build_port.yaml
    with:
      port-name: "OpenNotrium"
      short-name: OpenNotrium
      port-dir: opennotrium
      build-type: release
      email: ben@willmore.eu
