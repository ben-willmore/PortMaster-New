name: Build OpenEnroth
on:
  # Build on request
  workflow_dispatch
#  schedule:
#    - cron:  '5 4 * * *'

jobs:
  build:
    uses: ./.github/workflows/build_port.yaml
    with:
      port-name: "OpenEnroth"
      short-name: OpenEnroth
      port-dir: openenroth
      build-type: release
      email: ben@willmore.eu
